#include "main.h"
/**
 * _isdigit - show the abc's hmm
 * @c: get corespanadin ascii to entered char hmm
 * Return: 1 if lower 0 if not hmm
 */
int _isdigit(int c)
{
	if (c > 47 && c < 58)
		return (1);
	else
		return (0);
}
