#include "main.h"
/**
 * _puts - does something ...
 * @s: is some engtry
 * Return: return lenth
 */
void _puts(char *str)
{
	int n = 0;

	while (s[n] != '\0')
	{
		_putchar (str[n]);
		n++;
	}
	_putchar ('\n');
	return (n);
}
